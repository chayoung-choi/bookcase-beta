import React from "react";

const Error = ({match, location}) => {
    console.log(match);
    console.log(location);
    return (
        <>
            <>ERROR</>
        </>
    );
}
export default Error;
